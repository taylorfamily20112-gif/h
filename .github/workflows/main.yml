name: Build Geode Mod on: push: workflow_dispatch: jobs: build: runs-on: windows-latest steps: - uses: actions/checkout@v4 - name: Build the mod uses: geode-sdk/build-geode-mod@main id: build with: sdk: v5.10.1 - name: Upload build output uses: actions/upload-artifact@v4 with: name: more-checkpoints-mod path: ${{ steps.build.outputs.build-output }}
